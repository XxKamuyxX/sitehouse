import { Calendar } from './Calendar';

// This will redirect to the existing Calendar component
// AdminCalendar will be enhanced with drag & drop functionality later
export function AdminCalendar() {
  return <Calendar />;
}
